services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      - partnership-leads-data:/app/data
    restart: unless-stopped

volumes:
  partnership-leads-data:
    name: partnership-leads-data
